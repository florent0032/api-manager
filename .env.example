# ─── API Manager 环境变量配置 ───
# 复制此文件为 .env 并修改你需要的配置

# 后端端口 (FastAPI/Uvicorn)
BACKEND_PORT=8080

# 前端端口 (Next.js)
FRONTEND_PORT=3030

# API 基础地址 (前端调用后端的地址)
# 通常不需要修改，除非后端部署在其他机器上
NEXT_PUBLIC_API_BASE=http://localhost:8080/api
